apply plugin: 'com.android.application'

android {
    compileSdkVersion 33

    defaultConfig {
        applicationId "com.bhakti.hook"
        minSdkVersion 21
        targetSdkVersion 33
        versionCode 1
        versionName "1.0"

        // Harus ini biar LSPosed bisa deteksi modul
        buildConfigField "boolean", "XPOSED", "true"
    }
}
